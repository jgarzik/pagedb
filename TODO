
- checkpoint operation to update tables

- instead of one table-wide log cache, create per-block log caches.
  makes key iteration easier.

- do not overwrite log_idx+1, if multiple logs exist at startup

- correctly cache 
	put 'foo'
	del 'foo'
	put 'foo'

